requests>=2.34.2
ddgs>=9.0          # schluessellose Websuche (MIT) — siehe web.DuckDuckGoSearch
faster-whisper>=1.0   # lokales Hoeren (MIT) — siehe talos/hearing.py
cryptography>=50.0.1  # Ed25519 fuer die Release-Signatur (updater.py). Bewusst eine
                     # gepflegte Bibliothek statt eigener Krypto: das Update ist die
                     # eine Stelle, an der ein Fehler fremden Code ausfuehrt.
pypdf>=6.16.1        # PDFs lesen (BSD) — siehe talos/documents.py. Nur fuer PDF:
                     # docx/xlsx/pptx sind ZIP+XML und werden mit der
                     # Standardbibliothek gelesen, ohne weitere Abhaengigkeit.
                     # Untergrenze 6.16.1: alles darunter traegt PYSEC-2026-3910
                     # bis -3913 (CVSS 4.8-6.9). Eine NEUE Abhaengigkeit wird
                     # nicht mit bekannten Luecken eingefuehrt.
idna>=3.15           # transitiv ueber requests. Untergrenze 3.15: alles darunter
                     # traegt PYSEC-2026-215 (CVSS 6.9). Dieselbe Regel wie bei pypdf:
                     # keine Installation mit bekannter Luecke, auch nicht transitiv.
anyio>=4.14.2        # transitiv ueber httpx. Untergrenze 4.14.2: alles
                     # darunter traegt GHSA-82r6-8w77-94w6 (CVSS 9.3) und
                     # GHSA-5p39-cfhj-2xmp (CVSS 6.8). Dieselbe Regel wie bei idna.
